#include "main.h"

/**
* _abs - returns the absolute value of any input
* @n: Inout parameter
* Return: Result of the evaluation
*/

int _abs(int n)
{
	if (n > 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
