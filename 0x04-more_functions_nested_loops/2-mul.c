#include "main.h"

/**
 * main - multiplies two integers
 * @a: one of the integers that is multiplied
 * @b: the other integer being multiplied
 * Return: product of multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
